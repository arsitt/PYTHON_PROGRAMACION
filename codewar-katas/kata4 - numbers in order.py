arr = ['1','2','3','4','5']
def in_asc_order(arr):
    first = arr[0]
    for i in arr:
        if i == first:
                print('Es eso')
        elif i not:
            print('Hasta luego')
in_asc_order(arr)
    # for i in arr:
    #     if i == first:
    #         print('jeje')
    #     elif i += int(first): q